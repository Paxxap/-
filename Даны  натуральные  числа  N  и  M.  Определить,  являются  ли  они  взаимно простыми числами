<?php

$N = 17;
$M =11; 

if ($N < $M) 
    {
       $a=$N;
       $b=$M; 
    }
else
    {
       $a=$M;
       $b=$N;  
    }

for ($i=2; $i<=$a/2; $i++)
{
	if ($b % $i == 0 && $a % $i == 0)
	{
		echo "Числа $a, $b - не взаимно простые";
		die();
	}
}
echo "Числа $a, $b - взаимно простые";
?>
