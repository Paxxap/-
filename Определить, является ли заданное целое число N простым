<?php
$num = 37; 
$flag = 0;
for ($i=2; $i<=($num/2); $i++)
{
    if ($num % $i == 0)
    {
        echo "$num - не простое число";
        $flag++;
        break;
    }
}
if ($flag == 0)
{
    echo "$num - простое число";
}
